{
  "name": "planthood-recipe-site",
  "version": "1.0.0",
  "description": "Static site generator for Planthood recipes with Gantt chart visualization",
  "private": true,
  "scripts": {
    "scrape": "python scraper/scrape.py",
    "extract": "python -m planthood.cli extract",
    "enrich": "python -m planthood.cli enrich",
    "schedule": "python -m planthood.cli schedule",
    "quality": "python -m planthood.cli quality",
    "build-data": "npm run scrape && npm run extract && npm run enrich && npm run schedule",
    "build-site": "cd site && npm run build",
    "build": "npm run build-data && npm run build-site",
    "dev-site": "cd site && npm run dev",
    "setup": "pip install -e . && cd site && npm install",
    "test": "pytest"
  },
  "keywords": [
    "recipes",
    "planthood",
    "gantt",
    "static-site"
  ],
  "author": "",
  "license": "MIT"
}
